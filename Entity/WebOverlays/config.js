// HaloCaster configuration
// Edit these values to match your HaloCaster server

window.config = {
    // WebSocket host - the IP or hostname of your HaloCaster server
    // For local testing, use 'localhost' or '127.0.0.1'
    // For remote, use the server's IP address
    host: 'localhost',

    // WebSocket port - default HaloCaster port is 3333
    port: '3333',

    // Emblem service URL (VPS)
    emblemServiceUrl: 'http://104.207.143.249:3001',

    // Weapon icons base URL
    weaponIconsUrl: 'https://raw.githubusercontent.com/i2aMpAnT/CarnageReport.com/main/assets/weapons'
};
